The considered view combines an Enterprise Architecture model and a Requirements model (specified in a CSV file that can be opened in a spreadsheet such as Excel).
We want to use Requirements to comprehend the enterprise elements better.